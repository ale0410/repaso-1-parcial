import { ActorAltaPipe } from './actor-alta.pipe';

describe('ActorAltaPipe', () => {
  it('create an instance', () => {
    const pipe = new ActorAltaPipe();
    expect(pipe).toBeTruthy();
  });
});
